# SONIC 训练数据准备经验步骤：BVH 对齐、筛选与整理

本文总结 `sonic_bumi_full`（及其复用的 `SONIC_N3`/`SONIC_BUMI` 早期项目）在准备
BVH → Robot/SMPL 训练数据时踩过的坑和最终收敛的方法，整理成可以直接交给新机型
项目 Claude Code 参考的经验步骤文档。目标读者是**给新机型（新 embodiment）准备
SONIC 训练数据的 Agent**，不是描述某一次具体任务的记录。

结论先说：**不要跳过"BVH vs Robot retarget 对齐检查"这一步，直接把 retarget
产物和随便配上的 SMPL 数据拿去训练。** 本文档描述的三层过滤（时间对齐 → SMPL/Robot
帧数一致性 → 关节限位）在旧项目里排除了原始候选数据的约 **13.5%**
（112,903 条候选 → 最终 97,660 条可训练），如果跳过这层筛选，训练数据里会混入
大量重定向本身就错位的"垃圾"片段，且这种错位在肉眼快速预览时不一定能看出来。

---

## 0. 两条独立数据流：先分清楚"跟机型有关"和"跟机型无关"的部分

BVH（原始动作捕捉）之后会分叉成两条**完全独立**的处理流程：

```
                         原始 BVH（人体动作捕捉，NOVA-skeleton 或其他骨架）
                                      │
                ┌─────────────────────┴─────────────────────┐
                │                                            │
        [跟机型无关]                                  [跟机型强相关]
        BVH → SOMA 26 关节骨架 → SMPL 拟合                BVH → IK/retarget → 机器人关节角度
        （pose_aa/transl/smpl_joints，通用人体表示）        （npz/csv，每个机型独立一份）
                │                                            │
                └─────────────────┬──────────────────────────┘
                                   │
                    按 {date}/{clip_name} 这类 key 配对
                                   │
                    对齐检查、帧数修复、关节限位筛选
                                   │
                          最终 motion_lib PKL（robot + smpl 配对）
```

**关键判断依据**（来自 `SONIC_BUMI/HANDOFF.md` §6 的实测教训）：

- **机器人侧**（BVH → npz）需要做 **Y-up/cm → Z-up/m** 坐标变换。
- **人体侧**（BVH → SMPL）**不做坐标轴变换**，只做 cm→m 单位换算（SMPL 的 `transl`
  字段本身就是 Y-up 约定，下游会自己处理）。
- 历史上真实踩过的坑：第一次做这件事时，把机器人那条线已经验证过的 Z-up
  变换矩阵直接搬去处理 SMPL 数据——因为"都是同一份 BVH 数据"就想当然认为终点坐标
  约定也一样。**这个错误不会报错，只会让训练出来的模型姿态整体错误但很难第一时间
  发现**，务必在写代码前就把这两条线的坐标约定分别写清楚、分别测试。

**对新机型项目的直接推论**：如果新机型和已有项目（BUMI3/N3）用的是**同一批
BVH 源动作**，SMPL 侧数据原则上可以直接复用/软链，不需要重新做 BVH→SMPL；
只需要重新做机器人侧的 retarget，再重新跑一遍下面的对齐检查（因为新机型的
retarget 实现、关节配置和旧机型不一样，旧的对齐结果不能直接套用）。
`SONIC_BUMI` 项目的 SMPL 数据实际上就是软链自 `SONIC_N3/data/extracted/smpl_filtered`，
这是已经在生产环境里验证过的复用模式。

---

## 1. 第一层筛选：BVH ↔ Robot retarget 时间对齐检查

这是最容易被跳过、但最重要的一层。目的：确认 retarget 工具产出的机器人动作
在时间轴上真的和原始 BVH 对得上——retarget 过程可能引入固定的帧偏移（lag）、
局部丢帧、或者整体时长对不上，这些问题**光看关节角度数值范围通常看不出来**，
需要专门检测。

### 1.1 候选集合

对 BVH 和 retarget 产物的 key 做交集（不要抽样，做全量——旧项目对 112,903
条候选做了全量检查，不是抽样验证）。

### 1.2 三项检查

**检查 1：时长一致性**

```
residual = n_target - round(n_bvh * target_fps / source_fps)
通过条件：|residual| <= 1
```

**检查 2：起点 lag（时间偏移）**

- 主信号：BVH 根关节（如 Hips，注意用真实根关节，不是恒零的哑关节）的 3D
  位移速度模长，与 retarget 产物的根节点线速度模长，在目标帧率网格上重采样后，
  扫描 `lag ∈ [-50, +50]` 帧找最佳相关性。
  通过条件：`|best_lag| <= 1` 且 `|corr0| > 0.95`。
- **动态范围阈值**：如果根节点全程 3D 位移路径长度太短（原项目用 0.5m 作为阈值，
  这个数字是从 800 条随机 clip 的实测统计里量出来的，不是拍脑袋定的——低运动量
  clip（idle/looking_around 等）的路径长度 90 分位数约 0.46m，其余 clip 的 25 分位数
  约 0.52m，两类在 0.5m 附近有明显分离），根信号本身的相关性检测会不可靠，此时
  切换到 fallback 信号。**新机型必须重新用类似方法统计自己的动作库分布，不能直接
  套用 0.5m 这个数字**——不同机型/不同动作库的运动幅度分布不一样。
- fallback 信号：全身（剔除手指、下颌、眼睛、脚趾等末端标记）旋转活动度信号
  `sqrt(Σ角速度²)`。**角速度务必用四元数测地距离计算，不要用原始 Euler
  角差分**——Euler 差分在接近 ±90° 俯仰时会产生数千 deg/s 的伪极值，实测曾把
  一条 clip 的相关系数从可用直接拉到 0.002。fallback 阈值只看 `best_corr > 0.5`
  （不再要求特定 lag），因为 fallback 信号本身精度不足以精确定帧，只用来粗筛
  "是否存在明显整体错位"。

**检查 3：分段一致性**（排除"整体对上了，但中间局部错位"的情况）

- 按目标帧数分层分段：`>=600` 帧（12s）分 4 段，`>=300` 帧（6s）分 2 段，更短跳过。
- 推荐判据（比"各段 lag 差 <=1 且 corr 均 >0.9"这种严格判据更好，旧项目验证过
  能减少 87% 的误杀且不损失真实丢段检测率）：
  ```
  通过 = NOT(lag 漂移幅度 >= 4 帧 OR 任一段 corr < 0.5 OR 相邻段 corr 落差 > 0.35)
  ```

### 1.3 阈值不能拍脑袋定，必须用数据校准

这是旧项目里最值得借鉴的方法论，不是具体数字：

1. **检测灵敏度检验**：取一批"已知良好"的 clip（比如根信号相关性 >0.97 的），
   人为注入已知的 3/5/10/20 帧偏移，重跑整套 lag 检测流程，确认注入的偏移能被
   正确识别、且严格判据不会被"意外放行"。旧项目在万次注入实验中做到了 0% 的
   漏检率。**新机型必须用自己的 retarget 数据重做这组实验**，不能假设方法在
   新机型上依然有同样的灵敏度。
2. **富集分析定阈值**（base-rate enrichment）：构造几万条"确定错误"的随机配对
   （把真实 key 打乱重排，保证不是真实配对），跑同样的检测流程，统计"纯属巧合"
   通过某个阈值的概率；再看真实候选集合在该阈值区间的占比，两者相除得到"富集倍数"。
   富集倍数远大于 1（旧项目用 ≥35 倍作为可信线）说明这个特征代表真信号而不是巧合，
   据此定档位分界，而不是凭感觉选一个相关系数阈值。
3. **周期性/loop 动作的专项测试**：互相关方法对周期性动作（走路循环、舞蹈循环）
   有天然的"走样"风险（真错位可能被误判成对齐良好）。必须专门构造循环类样本做
   同样的注入偏移测试，确认方法在这类数据上依然可靠。

### 1.4 分档，不要做二元 pass/fail

把结果分成至少三档（旧项目用了 A/B/C/D 四档）：

| 档位 | 含义 | 处置建议 |
|---|---|---|
| 高置信 | 主信号强相关、lag 稳定、分段一致 | 直接进训练集 |
| 中置信 | 主信号较弱但明显优于随机基线，或 fallback 信号确认 | 进训练集，标注局限（如未做分段验证） |
| 需人工复核 | 处于灰色地带 | 数量少（旧项目约 1.36%）时可整体不用；数量大时应抽样人工看 |
| 确认错位/无法判定 | 明确 lag 超限，或双信号都太弱判不了 | 排除，不进训练集 |

二元判定会把很多"实际对齐但信号偏弱"的 clip 连带误杀（旧项目实测二元判定只能
用 75.81%，改成分档后可用比例提升到 93.13%，多出接近 2 万条数据），分档保留了
"备用池"的可能性，比直接删除更稳妥。

---

## 2. 第二层筛选：SMPL / Robot 帧数一致性

即使 BVH↔Robot 对齐检查通过了，官方或复用的 SMPL PKL 也可能和同名 Robot PKL
帧数对不上——旧项目实测约 **2/3** 的官方 SMPL PKL 比同名 Robot PKL 少 1~2 帧。

**这个问题不会在数据准备阶段报错，会在训练启动时以 assert 崩溃的形式出现**
（motion_lib 断言 SMPL 与 Robot 运行时帧数相等），所以必须在离线阶段主动检测
并修复，不能等训练报错才发现。

修复方式：对帧数不匹配的配对做重采样/裁剪对齐到统一目标帧数（保留完整改动日志，
每条改了什么、改了几帧，方便审计）；不要静默丢弃这些 clip——2/3 的比例太高，
直接丢弃会损失大量可用数据。

SONIC 训练端本身有一个运行时安全网（`paired_frame_alignment: mode=trim_trailing,
max_frame_delta<=2`，允许运行时裁掉 0-2 个尾帧），但这**只应该当保险丝用**，
不能当成数据准备阶段的主要对齐手段——安全网只能吸收 1-2 帧的误差，吸收不了
系统性问题，而且用运行时裁剪掩盖数据问题会让后续排查更困难。

---

## 3. 第三层筛选：物理合理性（关节限位等）

对齐检查解决的是"时间轴对不对"，不解决"retarget 出来的姿态本身是否超出机器人
物理限位"。需要单独一层检查：

- 遍历每条 clip 的关节角度轨迹，检查是否超出该机型的关节限位（URDF/MJCF 里的
  `joint_range`）。
- 记录违规帧占比，超过一定比例的 clip 直接剔除；轻微超限（可能是数值噪声或
  retarget 边界效应）可以考虑 clamp 到限位范围内而不是整条丢弃，但要记录哪些
  clip 被 clamp 过，clamp 幅度多大，方便后续追查训练异常时排查。

---

## 4. 坐标契约与关节顺序：写成契约，不要靠隐式假设

这一条不是"筛选"，但是所有上面几层检查能不能做对的前提：

- 每一份数据（BVH 源、Robot retarget 产物、SMPL 拟合结果）在正式写代码处理之前，
  先明确写清楚：坐标系是 Y-up 还是 Z-up、单位是 cm 还是 m、根关节朝向的参考约定、
  四元数顺序是 wxyz 还是 xyzw、关节顺序是按名称还是按列位置。
- **只信任显式的关节名称（`joint_names`）做重排，不要假设列位置固定**——不同批次
  的 retarget 产物即使是同一个机型，也可能因为来源 MJCF/URDF 版本不同而导致列
  顺序不同，按名称重排 + 名称集合校验（不允许有一个 robot 独有或缺失的关节名）
  能避免这类静默错位。
- 把这些契约写成可执行的校验代码（读取每个文件时先断言坐标约定、形状、单位在
  预期范围内），不要只写在文档里指望人记得住。

---

## 5. 索引构建：只做软链，不复制/不重写源文件

最终把通过全部筛选的 clip 组织成训练用的 motion_lib 索引时：

- 用**绝对软链接**指向原始（未修改的）源 PKL 文件，不复制、不重写、不重采样源文件——
  这样源数据可以被多个下游索引（不同机型、不同筛选版本）复用，且任何时候都能
  追溯到最初的源文件。
- 索引发布前先在独立 staging 目录构建，完整校验通过（计数、配对、SHA256）后再
  用原子操作（`os.replace`）切换成正式索引，避免半成品索引被训练进程读到。
- 每个发布的索引都要有 manifest（train/test JSONL）+ `summary.json` +
  `provenance.json`（记录来源路径、构建工具版本、关键 SHA256），`validate`
  子命令应该能独立重新计算全部 SHA256 并核对索引没有漂移。
- 已经确认没有训练/评估进程引用旧索引后才能删除旧索引；即使删除了索引本身，
  底层的三个原始来源目录也不能删——它们是唯一真源，索引没了可以用同样的构建
  命令重新生成。

---

## 6. 交给新机型项目 Claude Code 的执行清单

按顺序执行，每一步都要有实际验证证据（不是"应该没问题"）：

1. 确认新机型是否和已有项目共用同一批 BVH 源动作。共用则直接规划复用/软链
   现成 SMPL 数据；不共用则需要单独规划 BVH→SMPL 的拟合流程（这条链路和机型无关，
   可以先于机器人侧独立完成）。
2. 做新机型的 BVH → Robot retarget（这一步机型强相关，必须重新做，不能复用
   旧机型的产物）。明确写清楚这一步用的坐标变换（Y-up/cm → Z-up/m 还是别的约定）
   和目标帧率。
3. 拿新机型的 retarget 产物和 BVH 做全量对齐检查（§1），**不要复用旧机型校准出的
   具体阈值数字**，用同样的方法论（注入偏移灵敏度测试 + 富集分析）在新数据上
   重新校准。
4. 对通过对齐检查的 clip，做 SMPL/Robot 帧数一致性检查和修复（§2）。
5. 做关节限位等物理合理性检查（§3）。
6. 全程执行坐标契约的显式校验（§4），尤其是第一次跑通整条流程时，人工挑几条
   代表性 clip 用 MuJoCo/Isaac 可视化复核，不要只信数值检查。
7. 构建最终 motion_lib 索引（§5），跑 `validate` 确认计数、配对、哈希都对得上。
8. 把最终清单的规模、被排除的数量和排除原因写进项目自己的修改记录文档，方便
   后续排查训练效果问题时能先确认"是不是数据本身有问题"。

---

## 参考实现位置（旧项目，仅供查阅代码逻辑，不代表可以直接复用其具体阈值）

以下路径位于本机可访问的服务器 `/data/muyingchao/`（不在本仓库内，新项目需要
自行移植或重新实现，不要跨项目直接 import）：

- `SONIC_BUMI/tools/bvh_io.py`：BVH 读取。
- `SONIC_BUMI/tools/align_check.py`、`signals.py`：对齐检查主逻辑与信号计算。
- `SONIC_BUMI/tools/segment_v2_validate.py`：分段一致性检查 v2 判据。
- `SONIC_BUMI/tools/reclassify_full.py`：A/B/C/D 分档重判定。
- `SONIC_BUMI/tools/waist_yaw_distribution.py`：动态范围/阈值的统计分布分析。
- `SONIC_BUMI/tools/fix_smpl_robot_frame_mismatch.py`：SMPL/Robot 帧数修复。
- `SONIC_BUMI/tools/build_clamp_and_reject_manifests.py`：关节限位 clamp/拒绝清单。
- `SONIC_BUMI/tools/make_smpl_symlinks.py`：SMPL 软链接构建（含跨项目复用逻辑）。
- `SONIC_BUMI/reports/alignment_summary.md`、`reclassification_summary.md`：
  完整的方法说明、阈值推导过程和最终统计结果，是本文档最主要的依据来源。
- `SONIC_BUMI/HANDOFF.md`：项目交接文档，含坐标系踩坑记录（§6.4、§6.5）。

本仓库（`sonic_bumi_full`）内与之衔接的部分：

- `gear_sonic/data_process/extract_soma_joints_from_bvh.py`：BVH → SOMA 26 关节
  骨架（跟机型无关的通用人体处理）。
- `gear_sonic/tools/prepare_bumi3_sonic_dataset.py`：读取已经完成对齐/筛选的
  Robot+SMPL 配对数据，做关节重排、SMPL 50Hz 同步重采样，并做二次配对一致性
  校验（不做对齐本身，只做转换和校验）。
- `gear_sonic/utils/motion_lib/motion_lib_base.py` 的
  `resolve_paired_frame_alignment`：训练加载阶段的帧数容差安全网
  （`trim_trailing`，`max_frame_delta`），不是数据准备阶段的对齐机制。
